{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "chrome-helper",
  "title": "Chrome Helper",
  "description": "A collection of Chrome browser utilities: copy URL, copy Markdown link, extract HTML, and open in Atlas.",
  "icon": "extension-icon.png",
  "author": "ykpeng",
  "categories": [
    "Productivity",
    "Web",
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "copy-url",
      "title": "Copy Chrome URL",
      "description": "Copy the URL of the active Chrome tab to the clipboard",
      "mode": "no-view"
    },
    {
      "name": "copy-markdown-link",
      "title": "Copy Chrome Markdown Link",
      "description": "Copy the URL and title of the active Chrome tab as a Markdown link",
      "mode": "no-view"
    },
    {
      "name": "extract-html",
      "title": "Extract Chrome HTML",
      "description": "Extract and copy the body HTML of the active Chrome tab",
      "mode": "view"
    },
    {
      "name": "open-in-atlas",
      "title": "Open in Atlas",
      "subtitle": "Chrome Tab → Atlas",
      "description": "Open the active Chrome tab URL in ChatGPT Atlas browser",
      "mode": "no-view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.93.3",
    "@raycast/utils": "^1.19.1"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^22.13.4",
    "@types/react": "^18.3.3",
    "eslint": "^8.57.1",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "prepublishOnly": "echo \"Error: no publish\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}
